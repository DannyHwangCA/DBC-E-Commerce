<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
  <div class="container text-center">
    <div class="page-header">
      <h1>Hi <%= @user.username %></h1>
    </div>
    <p>
      Than you for shopping with us, here is your order detail
    </p>
  </div>
    <div class="container">
      <div class="row">
        <div class="col-lg-6">
          <% @collection.each do |c| %>
            <%= image_tag("money.jpg", class: "img-thumbnail", alt: "image placeholder", data: { src: "holder.js/200x200" }, width: '250', height: '250')%>
            <h2><%= c.product.name %></h2>
            <p>total: <%= c.total %></p>
            <p>quantity: <%= c.quantity %></p>
            <p>address: <%= c.address %></p>
            <p>phone: <%= c.phone %></p>
          <% end %>
        </div>
        <hr>
      </div>
    </div>
  </body>
</html>
